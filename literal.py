character = "C";
character = chr(ord(character)+1); # ord(character): Converts the character to its ASCII value.
#ord(character) + 1: Adds 1 to the ASCII value to get the next character in the sequence.
print (character);